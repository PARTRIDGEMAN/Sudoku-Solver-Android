org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# AGP 9 enables built-in Kotlin by default. This project intentionally uses the
# explicit Kotlin plugin so the Compose compiler and Kotlin versions stay pinned.
android.builtInKotlin=false
