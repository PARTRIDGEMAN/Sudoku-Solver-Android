# Phase 3

The implementation source of truth is `docs/PHASE3_CODEX_PROMPT.md` together with the requirements/test-matrix documents it references. The corresponding GitHub issue should link back to those files so implementation details remain versioned with the codebase.
