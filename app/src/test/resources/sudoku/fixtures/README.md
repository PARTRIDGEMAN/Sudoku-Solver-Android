# Sudoku vision fixtures

`approved/` contains the two user-approved JPEGs, a pinned checksum inventory, and
independently reviewed geometry. `APPROVED_URLS.txt` records their supplied URLs.
See `docs/FIXTURE_SOURCES.md` for inventory, provenance limits, and test policy.
The deleted `real/` corpus is not used. Test images must never be packaged in the APK.
